Фабрика метакласса, которая будет возвращать метакласс,
который будет приводить стиль наименования всех методов и свойств класса
к заданному (CamelSpace)


После установки зависимостей
pip install -r requirements.txt

Запустить brown.py из директории script (Установка corpa\brown из nltk)

Для тестирования используется pytest

camel.py - реализация разбивки сложного слова на простые
MetaCamel.py - релизация метакласа


Пример использования:
from MetaCamel import CodeStyle

class MyAwesomeClass(metaclass=CodeStyle):
    def some_method(self):
        pass

print(MyAwesomeClass.SomeMethod)
